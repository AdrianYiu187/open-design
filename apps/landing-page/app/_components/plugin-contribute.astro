---
/*
 * Reusable "contribute a plugin" callout shown on every /plugins/ page
 * (hub, category indexes, and detail pages). Points contributors at the
 * GitHub PR flow — `plugins/_official/` for the catalogue entry plus the
 * contribution guide. Pure link-out; no new repo files needed.
 */
import { getPluginsCopy } from '../_lib/plugins-i18n';
import { localeFromPath } from '../i18n';

const locale = localeFromPath(Astro.url.pathname);
const pcopy = getPluginsCopy(locale);

const CONTRIBUTE_URL =
  'https://github.com/nexu-io/open-design/tree/main/plugins/_official';
const GUIDE_URL =
  'https://github.com/nexu-io/open-design/blob/main/CONTRIBUTING.md';
---

<aside class="plugin-contribute">
  <div class="pc-text">
    <h2 class="pc-title">{pcopy.contributeTitle}</h2>
    <p class="pc-body">{pcopy.contributeBody}</p>
  </div>
  <div class="pc-actions">
    <a class="btn btn-primary" href={CONTRIBUTE_URL} target="_blank" rel="noopener">
      {pcopy.contributeCta}
    </a>
    <a class="pc-guide" href={GUIDE_URL} target="_blank" rel="noopener">
      {pcopy.contributeGuide}
    </a>
  </div>
</aside>

<style>
  .plugin-contribute {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 28px;
    flex-wrap: wrap;
    margin: 64px 0 0;
    padding: 28px 32px;
    border: 1px solid var(--line);
    border-radius: 14px;
    background: var(--paper-warm, #f5f5f5);
  }
  .pc-title {
    margin: 0 0 8px;
    font-size: 20px;
  }
  .pc-body {
    margin: 0;
    max-width: 560px;
    color: var(--ink-soft, #434343);
    line-height: 1.55;
    font-size: 14.5px;
  }
  .pc-actions {
    display: flex;
    align-items: center;
    gap: 18px;
    flex-shrink: 0;
  }
  .pc-guide {
    font-size: 14px;
    font-weight: 600;
    color: var(--ink, #262626);
    white-space: nowrap;
  }
  @media (max-width: 640px) {
    .plugin-contribute {
      padding: 22px;
    }
  }
</style>
